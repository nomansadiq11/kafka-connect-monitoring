apiVersion: v1
kind: Service
metadata:
  name: {{ include "kafka-monitoring.fullname" . }}
  labels:
    {{- include "kafka-monitoring.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kafka-monitoring.selectorLabels" . | nindent 4 }}
